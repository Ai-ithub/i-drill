# 🚀 راهنمای راه‌اندازی کامل i-Drill

## ✅ موارد نصب شده

1. ✅ **Node.js v24.11.0** و **npm v11.6.1**
2. ✅ **وابستگی‌های فرانت‌اند** (395 پکیج npm)
3. ✅ **وابستگی‌های بک‌اند Python** (FastAPI, uvicorn, و غیره)
4. ✅ **Docker Desktop** (نصب شده اما نیاز به تنظیمات)

---

## ⚠️ مشکلات و راه‌حل‌ها

### مشکل ۱: پورت‌های پیش‌فرض اشغال است

ممکن است پورت 8001 (بک‌اند) یا 5173 (فرانت‌اند) توسط برنامه دیگری اشغال شده باشد. برای حل:

**گزینه ۱: متوقف کردن برنامه اشغال‌کننده**
```powershell
# پیدا کردن Process ID (برای بک‌اند)
netstat -ano | findstr :8001

# متوقف کردن (به Administrator نیاز دارد)
Stop-Process -Id [PID] -Force
```

**گزینه ۲: استفاده از پورت دیگر**
اگر نمی‌توانید برنامه را متوقف کنید، سرویس‌ها را روی پورت‌های جایگزین اجرا کنید:
```powershell
cd src/backend
python -m uvicorn app:app --host 0.0.0.0 --port 8002
```
و برای فرانت‌اند:
```powershell
cd frontend
npm run dev -- --host 0.0.0.0 --port 5175
```

سپس مقدار `VITE_API_URL` یا تنظیمات مشابه را در فرانت‌اند متناسب با پورت جدید به‌روزرسانی کنید.

### مشکل ۲: Docker Desktop نیاز به تنظیمات

Docker Desktop نصب شده اما برای اجرای کامل نیاز دارد:

1. **فعال‌سازی WSL2** (Windows Subsystem for Linux 2)
2. **فعال‌سازی Virtualization** در BIOS
3. **Restart سیستم** (توصیه می‌شود)

**راه‌اندازی Docker پس از تنظیمات:**
```powershell
# راه‌اندازی Docker Desktop (manually از Start Menu)
# سپس بررسی:
docker ps

# راه‌اندازی Kafka و Zookeeper
cd [project-root]
docker-compose up -d
```

---

## 🎯 راه‌اندازی سریع (بدون Docker - برای تست UI)

### ۱. راه‌اندازی فرانت‌اند

```powershell
cd frontend
npm run dev -- --host 0.0.0.0 --port 5173
```

فرانت‌اند روی `http://localhost:5173` اجرا می‌شود (در صورت نیاز می‌توانید پورت را تغییر دهید).

### ۲. راه‌اندازی بک‌اند

**ابتدا پورت 8001 را آزاد کنید** (یا از پورت دیگری استفاده کنید):

```powershell
cd src/backend
python -m uvicorn app:app --host 0.0.0.0 --port 8001 --reload
```

بک‌اند روی `http://localhost:8001` اجرا می‌شود.

### ۳. بررسی کارکرد

- **بک‌اند:** http://localhost:8001/docs (Swagger UI)
- **فرانت‌اند:** http://localhost:5173
- **Health Check:** http://localhost:8001/api/v1/health

---

## 🔧 راه‌اندازی کامل (با Docker - برای تولید)

### ۱. تنظیم Docker Desktop

1. Docker Desktop را از Start Menu باز کنید
2. اگر خطا داد، WSL2 را نصب کنید:
   ```powershell
   wsl --install
   ```
3. سیستم را Restart کنید

### ۲. راه‌اندازی Docker Services

```powershell
cd [project-root]
docker-compose up -d
```

این دستور سرویس‌های زیر را راه‌اندازی می‌کند:
- Zookeeper (2181)
- Kafka (9092/29092) با health-check
- PostgreSQL (5432) با کاربر `drill_user` و گذرواژه `drill_password`
- FastAPI (8001) با Rate Limit و متغیرهای محیطی توسعه
- Producer (برای تولید داده)
- Consumer (برای مصرف داده)

### ۳. راه‌اندازی بک‌اند و فرانت‌اند

```powershell
cd frontend
npm run dev
```

سرویس FastAPI داخل Docker روی پورت 8001 اجرا می‌شود؛ فرانت‌اند را مطابق مرحله‌ی قبل اجرا کنید یا با پورت دلخواه (مثلاً 5173/5175) بالا بیاورید.

---

## 📝 نکات مهم

1. **برای اجرای کامل با داده‌های واقعی:**
   - Docker Desktop باید فعال باشد
   - Kafka و Zookeeper باید در حال اجرا باشند
   - Database (PostgreSQL) باید راه‌اندازی شود

2. **برای تست UI بدون داده:**
   - فقط بک‌اند و فرانت‌اند را اجرا کنید
   - برخی ویژگی‌ها (مانند WebSocket) ممکن است کار نکنند

3. **مشکلات رایج:**
   - **Import Error:** مطمئن شوید همه dependencies نصب شده‌اند
   - **Port Already in Use:** پورت را تغییر دهید یا برنامه اشغال‌کننده را متوقف کنید (به ویژه 8001 و 5173)
   - **Docker Not Starting:** WSL2 و Virtualization را فعال کنید

---

## 🆘 کمک بیشتر

- مستندات API: `src/backend/API_README.md`
- راهنمای سریع: `src/backend/QUICKSTART.md`
- مستندات فرانت‌اند: `frontend/README.md`

---

**وضعیت فعلی:**
- ✅ Node.js و npm نصب شده
- ✅ وابستگی‌های فرانت‌اند نصب شده
- ✅ وابستگی‌های بک‌اند نصب شده
- ✅ Docker Desktop نصب شده (نیاز به تنظیمات)
- ⚠️ پورت 8000 اشغال است (نیاز به آزادسازی)
- ⚠️ Docker Services نیاز به راه‌اندازی دارند
