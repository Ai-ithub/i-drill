# ⚡ راهنمای سریع به‌روزرسانی FastAPI به 0.116.x

این راهنمای سریع برای به‌روزرسانی FastAPI از نسخه فعلی (0.104.1) به 0.116.x است.

---

## 📋 وضعیت فعلی

- **نسخه نصب شده**: 0.104.1
- **نسخه هدف**: 0.116.x
- **وضعیت**: ✅ سازگار و آماده به‌روزرسانی

---

## 🚀 مراحل به‌روزرسانی (3 مرحله)

### مرحله 1: به‌روزرسانی requirements.txt

فایل `requirements/backend.txt` را ویرایش کنید:

```diff
- fastapi>=0.121.0,<0.122.0
+ fastapi>=0.116.0,<0.117.0
```

### مرحله 2: نصب وابستگی‌ها

```bash
# فعال کردن virtual environment
source venv/bin/activate  # Linux/Mac
.\venv\Scripts\activate   # Windows

# به‌روزرسانی FastAPI
pip install --upgrade "fastapi>=0.116.0,<0.117.0"

# بررسی نسخه نصب شده
python -c "import fastapi; print(fastapi.__version__)"
# باید: 0.116.x

# بررسی conflicts
pip check
```

### مرحله 3: تست

```bash
# تست اجرای سرور
cd src/backend
uvicorn app:app --reload

# در ترمینال دیگر
curl http://localhost:8001/api/v1/health

# تست با pytest
pytest tests/test_auth.py -v
```

---

## ✅ بررسی سریع

### چک‌لیست 5 دقیقه‌ای:

- [ ] `pip install --upgrade "fastapi>=0.116.0,<0.117.0"` بدون خطا
- [ ] `pip check` بدون conflict
- [ ] `python -c "import fastapi; print(fastapi.__version__)"` نشان می‌دهد 0.116.x
- [ ] `uvicorn app:app --reload` بدون خطا اجرا می‌شود
- [ ] `curl http://localhost:8001/api/v1/health` پاسخ می‌دهد

---

## ⚠️ نکات مهم

1. **هیچ breaking change مهمی وجود ندارد** بین 0.104.1 و 0.116.x
2. **کد موجود بدون تغییر کار می‌کند**
3. **تمام ویژگی‌های فعلی (lifespan, middleware, WebSocket) سازگار هستند**

---

## 🔧 عیب‌یابی سریع

### مشکل: Import Error
```bash
pip install --force-reinstall "fastapi>=0.116.0,<0.117.0"
```

### مشکل: Version Conflict
```bash
pip check
# اگر conflict داشت، به‌روزرسانی dependencies مرتبط
pip install --upgrade "starlette>=0.47.2,<0.48.0"
```

### مشکل: Runtime Error
```bash
# بررسی logs
uvicorn app:app --reload --log-level debug
```

---

## 📚 مستندات کامل

برای تحلیل کامل و جزئیات بیشتر، به فایل زیر مراجعه کنید:
- `docs/FASTAPI_0.116_UPGRADE_ANALYSIS.md`

---

**زمان تقریبی**: 5-10 دقیقه  
**ریسک**: 🟢 پایین  
**وضعیت**: ✅ آماده برای production

