# I-Drill Project Environment Variables Example
# ---------------------------------------------
# To use this file:
# 1. Copy it to a new file named .env (cp .env.example .env)
# 2. Fill in the actual values in the new .env file.
# 3. The .env file is gitignored and should NEVER be committed.

# --- PostgreSQL Configuration ---
POSTGRES_USER=idrill_user
POSTGRES_PASSWORD=changeme
POSTGRES_DB=idrill_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# --- InfluxDB 2.x Initial Setup ---
# These are used by the official InfluxDB image on first run
DOCKER_INFLUXDB_INIT_MODE=setup
DOCKER_INFLUXDB_INIT_USERNAME=idrill_user
DOCKER_INFLUXDB_INIT_PASSWORD=changeme
DOCKER_INFLUXDB_INIT_ORG=i-drill-org
DOCKER_INFLUXDB_INIT_BUCKET=i-drill-bucket
DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=changeme_this_super_secret_token

# --- InfluxDB Client Configuration ---
# These are used by the Python application to connect to InfluxDB
INFLUX_URL=http://influxdb:8086
INFLUX_TOKEN=changeme_this_super_secret_token
INFLUX_ORG=i-drill-org
INFLUX_BUCKET=i-drill-bucket

# --- Kafka Configuration ---
KAFKA_BOOTSTRAP_SERVERS=kafka:9092